// environment.ts – Development environment configuration
// Angular CLI replaces this file with environment.prod.ts when building with
// the --configuration=production flag. Keep development-only values here.

export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
};